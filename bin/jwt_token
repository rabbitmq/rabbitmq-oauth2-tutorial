#!/usr/bin/env bash

SCRIPT="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

$SCRIPT/jwt_token.py ${1:?key name} $SCRIPT/../conf/${2:?name of the private key file} \
    $SCRIPT/../conf/${3:?name of the public key file} ${4:-some-client}
